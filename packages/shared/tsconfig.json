{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // tsup emits the build (ESM + CJS + declarations, ADR-14); tsc's job here is to typecheck only.
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "tsup.config.ts", "vitest.config.ts"]
}
