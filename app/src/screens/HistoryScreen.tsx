import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function HistoryScreen() {
  return (
    <PlaceholderScreen
      phase="Phase 10"
      summary="Attempts grouped by source image, the accuracy, latency and cost comparison across the four methods, and the JSON export."
    />
  );
}
