import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function CaptureScreen() {
  return (
    <PlaceholderScreen
      phase="Phase 05"
      summary="Photo capture, the on-device ML Kit path over both image variants, and the upload that every server engine is later re-run against."
    />
  );
}
