import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function LibraryScreen() {
  return (
    <PlaceholderScreen
      phase="Phase 06"
      summary="The stored image set, browsable and re-runnable, so any method can be measured again over old photos without re-shooting them."
    />
  );
}
