import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function BarcodeScreen() {
  return (
    <PlaceholderScreen
      phase="Phase 04"
      summary="EAN-13 scanning with vision-camera's useCodeScanner, decode latency measured from screen-ready to callback and recorded server-side."
    />
  );
}
